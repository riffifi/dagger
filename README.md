# Dagger Compiler

A minimal C++ scaffold for the Dagger language compiler targeting x86-64.

## Build

```bash
mkdir -p build
cd build
cmake ..
cmake --build .
```

## Run

```bash
./dagc path/to/source.dag
```
